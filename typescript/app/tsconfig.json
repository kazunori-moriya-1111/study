{
  "compilerOptions": {
    /* Basic Options */
    // "incremental": true,                   /* Enable incremental compilation */
    "target": "es6", /* Specify ECMAScript target version: 'ES3' (default), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019' or 'ESNEXT'. */
    "module": "commonjs", /* Specify module code generation: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', or 'ESNext'. */
    // jsファイルをコンパイル対象にするか
    // "allowJs": true
    // jsファイルもtsファイルのようにチェックするか
    // "checkJs": true
    // コンパイル後のjsファイルの出力先
    // outDir: ""
    // コンパイル元のディレクトリを指定
    // rootDir: ""
    // 使用していないローカル変数への警告
    // "noUnusedLocals" : true
    // 使用していない引数への警告
    // "noUnusedParameters" : true
    // 暗黙的なreturnを許可しない
    // "noImplicitReturns"
  },
  // コンパイル対象を指定する
  "include": [
    "index.ts",
    "tmp/compiler.ts"
  ],
  "exclude": [
    // "**/compiler.ts", /*どのディテクトでもコンパイル対象外にする*/
    "*.spec.ts",
    "node_modules"
  ],
  // 相対パス or 絶対パスでファイルを指定する excludeに指定されたファイルでもコンパイルできる
  // "files": []
}
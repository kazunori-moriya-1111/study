version: '3'

services:
  app:
    build: ./node
    ports:
      - 8080:8080
      - 3000:3000
    volumes:
      - ./app:/usr/src/app
      - ./react_app:/usr/src/react_app
      - ./react_todo:/usr/src/react_todo
    tty: true
    stdin_open: true
    command: /bin/sh